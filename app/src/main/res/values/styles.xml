<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dialog Title Style -->
    <style name="DialogTitleStyle" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">50dp</item>
        <item name="android:layout_marginStart">50dp</item>
    </style>

    <!-- Save Button Style -->
    <style name="SaveButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/btnbg</item>
    </style>

    <!-- Radio Button Style -->
    <style name="RadioButtonStyle" parent="Widget.MaterialComponents.CompoundButton.RadioButton">
        <item name="buttonTint">@color/btnbg</item>
    </style>

    <!-- Radio Button Color Selector -->
    <style name="RadioButtonColor" parent="">
        <item name="colorControlNormal">@color/btnbg</item>
        <item name="colorControlActivated">@color/DarkBlue</item>
    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
    </style>
</resources>